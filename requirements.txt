# Detection
blenderproc
ultralytics  # YOLO from pip for convenience; still have repo for dev
opencv-python
numpy

# Registration
open3d

# Utils
matplotlib

# Optional: Install Grounded-SAM2 & TEASER++ from their repos
# (since they aren't on PyPI, you may need `pip install -e detection/grounded-sam2`)
